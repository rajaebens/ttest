build_settings:
    ignore:
        - "protected/runtime/"
setup:
    composer:
        action: "install"
test:
    php_unit:
        config:
            - "phpunit.xml"
        args: "--stderr"
        path: "tests/"